_research/
__pycache__/
*.pyc
.impeccable/

# Run workspaces are per-project output, not repository content. Worked examples that
# ship with the skill live under marketing-minthep/assets/examples/ instead.
runs/

# Scratch written by research subagents while verifying arithmetic. Not deliverables.
.tmpcolor/

# Reference material gathered while researching, never shipped. Two kinds have landed here and both
# are somebody else's property: photographs whose own README reserves copyright to the original
# owners, and vendor help-centre pages cached to verify a platform default. Reading them locally is
# the point. Publishing them out of a repository that serves GitHub Pages is not, so the directory is
# ignored by name rather than trusted to a careful commit.
research_assets/

# Scratch written by research subagents (fetched HTML, arithmetic checks). Not deliverables.
.tmp*/
.tmp_*
.vnres/
.vnmkt/
.tmp_viet/
undefined/

# My own orchestration harness and the batches it dispatches. It accelerates authoring the
# skill; it is not part of the skill, which stays pure marketing craft with no router
# dependency of any kind.
_work/

# API keys. The image endpoint takes a bearer token; it must never reach a commit.
.env
.env.*
*.key
_secrets/

# 1 MB bundle of the agent definitions, rebuilt on demand. Not skill content;
# scripts/install_global.py excludes it from installs for the same reason.
marketing-minthep/agents.zip
